# Overlord Project YAML File

info:
  name: "serv_from_git.project"
  version: "1.0.0"
  author: ""
  description: ""

catalogs:
- type: "git"
  url: "https://github.com/DeanoC/overlord_serv.git"

definitions:
- type: board.tangnano9k.stock
  ports:
  registers:
  drivers:
  triple: riscv32-none-elf
  width: 32
  gateware:
    type: inline
    inline:
      actions:
        - action: sources

components: []

defaults: {}

instances: []

connections: []

